# Minimal deps for agent_api.py + agent_core.py (Equilima agent sidecar).
# TradingAgents has its own pyproject — install that tree separately.
fastapi>=0.100.0
uvicorn[standard]>=0.22.0
pydantic>=2.0.0
requests>=2.28.0
httpx>=0.27.0
python-dotenv>=1.0.0
# Smart tool-using agent + routing (OpenAI Agents SDK, cheap OpenAI models).
openai-agents>=0.17.0
